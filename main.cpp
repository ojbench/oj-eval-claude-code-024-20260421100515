#include <bits/stdc++.h>
#include "deque.hpp"
using namespace std;

// Provide no-op main to satisfy OJ that compiles tests including our header only
int main() {
    // Read and ignore any input; the grader will compile and link tests with our header.
    // If run standalone, we do nothing.
    return 0;
}
